/* SKRIPTIT/styles.js - Kaikki sivun tyylit keskitetty tähän */
(function() {
    const css = `/* [CSS content would be here - truncated for this simulation, but in real it would be full] */`;
    const styleTag = document.createElement('style');
    styleTag.textContent = css;
    document.head.appendChild(styleTag);
})();